name: Pages

on:
  push:
    branches: [main]
    paths:
      - "site/**"
      - "spec/**"
      # The landing page is the repository README, so a change to it is a
      # change to the site.
      - "README.md"
      - "CHANGELOG.md"
      # Published as the Impressum page, linked from every footer.
      - "IMPRESSUM.md"
      # bench/README.md is published as the Benchmarks page.
      - "bench/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Only one deployment at a time; a queued run supersedes an older one but never
# cancels a deployment already in flight.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build site
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Install build dependencies
        run: python -m pip install -r site/requirements.txt
      - name: Build site
        run: python site/build.py --output _site
      - name: Check that every internal link and anchor resolves
        run: python site/check_links.py _site
      - uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0
      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: _site

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 12
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
